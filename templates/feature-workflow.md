## Feature workflow

Before changing a Git repository, load and follow the installed Agent Skill
named `feature-workflow`. It must complete the work autonomously, including
committing, pushing, merging, and cleanup.
