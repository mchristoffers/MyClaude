interface:
  display_name: "Deploy Master-1 Compose"
  short_description: "Manage master-1 Compose apps, domains, and Actions deploys"
  default_prompt: "Use $deploy-master1-compose to deploy and maintain this private-repository Docker Compose application on master-1, including exact domain bindings, optional staging, and GitHub Actions triggers."
