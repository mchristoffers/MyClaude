## Feature workflow

Before implementing a new feature, load and follow the installed Agent Skill
named `feature-workflow`. It must complete the feature autonomously, including
committing, pushing, merging, and cleanup.